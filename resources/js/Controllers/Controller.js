/**
 * Abstract class to define the main methods
 */
export class Controller {
    /**
     * Method invoked by the router when resolving the route own by the controller
     */
    start() {

    }

    /**
     * Method invoked by the router when changing the route from the one owned by the controller
     */
    destroy() {

    }

}
